# Frontier Research Stage Handoff — Stage G / 2025-Q3

## Eligible findings
- package/runtime/platform identity belongs to renderer provenance;
- wrapper and underlying renderer-engine revisions should remain distinguishable;
- browser/export-engine identity can materially affect a delivered figure artifact;
- interactive/tooltip semantics are version-sensitive communication state;
- declarative recipe identity does not fully determine the realized interactive artifact.

## Unsafe generalization
- wheel availability => runtime tested;
- wrapper support => backend/browser availability;
- Chrome acquisition helper => browser reproducibility;
- render/export success => scientific validity;
- cursor/tooltip change => accessibility conformance;
- release notes => local render evidence.

```text
handoff != scientific authority
render state != scientific truth
interaction behavior != accessibility certification
publisher target != acceptance
```
