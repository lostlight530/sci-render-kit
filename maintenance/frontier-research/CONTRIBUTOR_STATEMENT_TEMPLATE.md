# Frontier Research Contributor Statement — TEMPLATE

> Use when multiple people/agents materially contribute or when responsibility needs to be explicit. This record separates scholarly/research responsibility from software/model/tool instrumentation.

## 0. Identity

- **Repository:** `lostlight530/sci-render-kit`
- **Stage / research product:** `<scope>`
- **Recorded:** `<date>`
- **Specification version:** `2026-09-19-first-batch`

## 1. Contribution principles

CRediT terminology may be used descriptively where appropriate, but this record does not determine authorship.

```text
contribution role != authorship determination
responsibility != tool invocation
software/model instrument != human contributor
agent execution != scientific responsibility by itself
```

## 2. Responsible contributors

| Contributor ID/name | Identity type | Contribution role(s) | Scope of contribution | Evidence/record | Responsibility note |
|---|---|---|---|---|---|
| `<C1>` | `<human / organization / declared agent identity / other>` | `<conceptualization / methodology / investigation / data curation / formal analysis / validation / writing / supervision / project-native role>` | `<scope>` | `<refs>` | `<note>` |

Use observed or declared identities only. Unknown stays `UNKNOWN`.

## 3. Instruments and research tools

| Instrument ID | Tool/model/search surface/script | Version/identity when observed | Activity supported | Output used | Limitations |
|---|---|---|---|---|---|
| `<I1>` | `<tool>` | `<version/UNKNOWN>` | `<search/extraction/analysis/etc.>` | `<output>` | `<limits>` |

Do not infer hidden provider/model identity from output style or platform name alone.

## 4. AI/agent contribution boundary

For any AI/agent materially involved, state:

- task assigned;
- observed system/model identity when available;
- whether source retrieval or browsing was performed;
- whether outputs were independently checked;
- who held final research/governance responsibility;
- known limitations or failed executions.

## 5. Validation / review contributions

Separate production from validation. A contributor who produced the research and reviewed it is not an independent reviewer unless a separate independent process occurred.

## 6. Attribution changes

Record later corrections to contribution attribution rather than silently rewriting responsibility history.
